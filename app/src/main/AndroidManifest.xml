<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="divyanshu.aclaglid.in.aclagliddemos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" > -->


        <!-- </activity> -->


        <!-- <activity android:name=".ListViewExample.AcadgildListView"> -->


        <!-- </activity> -->
        <!-- <activity android:name=".ListViewExample.DynamicListView"> -->


        <!-- </activity> -->
        <activity android:name=".TimePickerExample" />

        <!-- </activity> -->
        <!-- <activity android:name=".GridViewExmple"> -->


        <!-- </activity> -->

        <activity android:name=".ExplicitIntent.Activity1" />
        <activity android:name=".ExplicitIntent.Activity2" />
        <activity android:name=".PendingIntentExample.AlarmManagerExample"></activity>

        <receiver android:name=".PendingIntentExample.MyBroadcatReceiver" />

        <activity android:name=".SharedPreferences.Activity_SharedPreferences"></activity>
        <activity android:name=".SharedPreferences.ShowActivity" />
        <activity android:name=".SharedPreferences.SettingActivity" />
        <activity android:name=".Dialogs.DialogActivity"></activity>
        <activity android:name=".Dialogs.CustomDialog"></activity>
        <activity android:name=".SplashScreen">

        </activity>
        <!-- This meta-data tag is required to use Google Play Services. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".Advertisment"
            android:label="@string/title_activity_advertisment"></activity> <!-- Include the AdActivity configChanges and theme. -->
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />

        <activity android:name=".FragmentExample.FragmentLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FragmentExample.DatailsActivity"></activity>
    </application>

</manifest>